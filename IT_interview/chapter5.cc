#include <iostream>

using namespace std;
int i=1;
int main(int argc, char const *argv[])
{
    /****************$5.1 e.g. 1*****************/
    int i = i;
    cout << i << endl;
    /************End $5.2 e.g. 1*****************/

    /****************$5.2 e.g. 1*****************/
    // int a,x;
    // for(a=0,x=0; a<=1 && !x++; a++)
    //     a++;
    // cout << a << x << endl; 
    // for(a=0,x=0; a<=1 && !x++; )
    //     a++;
    // cout << a << x << endl; 
    /***********End  $5.2 e.g. 1*****************/
    cout << "Main Finished!" << endl;
    return 0;
}